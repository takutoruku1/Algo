/*
 * config.js
 * --------------------------------------------------------------------------
 * スプライトシートとアニメーションの設定をここにまとめています。
 * 本物の画像 (assets/character.png) を用意したら、この数値を実際の画像に
 * 合わせて変更してください。コードの他の部分はいじらなくて大丈夫です。
 * --------------------------------------------------------------------------
 */

// スプライトシート 1 枚の構成
const SPRITE_CONFIG = {
  key: 'hero', // ゲーム内でのテクスチャ名（変更不要）
  path: 'assets/character.png', // 画像ファイルの場所

  // シートが何列 × 何行で並んでいるか
  columns: 4,
  rows: 5,

  // 1 コマ（1 フレーム）のピクセルサイズ。
  // 画像全体が 1242x1242 なら 1242/4 ≒ 310、1242/5 ≒ 248 が目安。
  // 本物の画像に合わせて正確な値に直してください。
  frameWidth: 310,
  frameHeight: 248,

  // 画面に表示する際の拡大率（小さければ大きく、大きければ小さく）
  displayScale: 0.6,
};

// 4 列 × 5 行 = フレーム番号 0〜19 の対応表
//   0  1  2  3      ← 1 行目
//   4  5  6  7      ← 2 行目
//   8  9 10 11      ← 3 行目
//  12 13 14 15      ← 4 行目
//  16 17 18 19      ← 5 行目
//
// 実際の画像のどのコマが「歩き」「待機」「アクション」かは、画像を見ながら
// 下の frames 配列の番号を入れ替えて調整してください。
const ANIMATIONS = {
  // 立ち待機（ループ）
  idle: { frames: [0], frameRate: 4, repeat: -1 },

  // 歩行（ループ）。4・5 行目の歩いてるコマを使う想定。
  walk: { frames: [12, 13, 14, 15, 16, 17, 18, 19], frameRate: 10, repeat: -1 },

  // アクション（魔法を撃つ等）。3 行目を使う想定。1 回だけ再生。
  action: { frames: [8, 9, 10, 11], frameRate: 8, repeat: 0 },
};

// 移動の速さ（ピクセル / 秒）
const MOVE_SPEED = 220;
